import React from 'react';
import './Loading.css';

//Loading Component to show while the App is getting busy
const Loading = () => {
  return <div className="Loading">Loading…</div>;
};

export default Loading;
